package main

import (
	"fmt"
)

func main1() {
	/*
		server := initServer()
		command.LpushCommand(server, "blist", 321456)
		l := command.Llen(server, "blist")
		fmt.Println(server, "len of list ", l)
		gmap.Pt()
		var node = new(list.Node)
		ty := reflect.TypeOf(sds.Sds{})
		fmt.Println(ty.Size(), node.Next)
		var str = sds.Sds{Len: 100, Alloc: 200}
		fmt.Println("hello godis", str)
		sds.Pt(str)
	*/
}

func init() {
	fmt.Println("start ->")
}
